# 런타임 최소 버전 (단일 출처) — bootstrap.ps1 / bootstrap.sh 가 읽는다.
#
# 정책: "이상이면 기존 설치본을 그대로 재사용", 미만이거나 없을 때만 설치한다.
# (architecture.md §2 의 스택 기준. 버전 편차는 허용하되 하한선은 보장한다.)
MIN_PYTHON=3.13
MIN_NODE=24
MIN_PNPM=11
